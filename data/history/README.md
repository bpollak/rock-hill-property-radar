# Daily snapshots

Successful automated research runs write a dated JSON snapshot here before replacing `data/current.json`. Failed or invalid runs preserve the last successful snapshot, preventing the next run from marking every listing as new.
